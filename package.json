{
  "name": "best-3pl-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Best 3PL job tracking & billing portal",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --format=iife --outfile=public/app.js --loader:.js=jsx"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "resend": "^4.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "0.383.0"
  }
}
